using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class starerdialog : MonoBehaviour
{
    public GameObject game;

    // Start is called before the first frame update
    void Start()
    {
        game.SetActive(true);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
